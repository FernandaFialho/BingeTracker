import styled from 'styled-components';
import bglogin from './BackgroundHome.png';

export const AreaPag = styled.div`
    height: 768px;  
    background-image: url(${bglogin});
    background-repeat: no-repeat;

`;