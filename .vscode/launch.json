{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/backend/users/package.json",
                "localRoot": "${workspaceFolder}/backend/users"
            }
        }
    ]
}